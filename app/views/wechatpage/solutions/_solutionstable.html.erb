<!-- 如果确实有搜索东西 -->

  <!-- 用户搜到了 -->
  <% if @solutions.present? %>
    <div class="col-md-10 col-sm-10 col-xs-10 col-md-offset-1 col-sm-offset-1 col-xs-offset-1">
      <ul>
      <% @solutions.each do |s| %>
        <li><%= link_to(s.name,wechatpage_solution_path(s)) %></li>
      <% end %>        
      </ul>

    </div>

  <% end %>

